<script type="text/javascript">
    mixpanel.track("intro_diversification", {
        
    });
</script>
<script>
$(function() {
  $('a[href*="#"]:not([href="#"])').click(function() {
    if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'') && location.hostname == this.hostname) {
      var target = $(this.hash);
      target = target.length ? target : $('[name=' + this.hash.slice(1) +']');
      if (target.length) {
        $('html, body').animate({
          scrollTop: target.offset().top
        }, 1000);

        return false;
      }
    }
  });
});

</script>

<div class="edu-block">
  <div class="big-headline">
  Ok, a couple more quick questions to make sure we've covered the basics.
  </div>
  <div class="headline edu-para">In the world of investing, you hear the word "diversify" a lot. Do you want a quick overview of what it means to diversify your investments?
  </div>

<br /><a href="#anchor" class="next-button">Sure</a><br /><br />

<%= form_for @investing_profile, url: wizard_path, method: :put do |f| %>
	<%= f.hidden_field :explain_diversification, value: "Yes" %>
	<%= f.submit "No need", class:"next-button" %>
<% end %>
</div>

<div id="diversify"><a name="anchor"></a>
	<div class="edu-block"> 
	  <div class="big-headline">What does it mean to diversify your investments?</div>
		<div class="headline">
			<p class="edu-para">Diversifying essentially means spreading your investments across a large set of companies to lower your risk. </p>

			<p class="edu-para">For example, if you had all your money invested in one company and its stock plummets, you would lose a lot of money. </p>

			<p class="edu-para">To prevent this, you can invest your money in a diverse set of companies. Our recommended first investment will be fully diversified for this reason.</p>
		</div>

	    <%= form_for @investing_profile, url: wizard_path, method: :put do |f| %>
		<%= f.hidden_field :explain_diversification, value: "No" %>
		<%= f.submit "Got it", class:"next-button" %>
		<% end %>
	</div>
</div>


		
	</div>
</div>
