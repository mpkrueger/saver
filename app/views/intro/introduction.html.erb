<script type="text/javascript">
    mixpanel.track("intro_introduction", {
        
    });
</script>
<script>
$(function() {
  $('a[href*="#"]:not([href="#"])').click(function() {
    if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'') && location.hostname == this.hostname) {
      var target = $(this.hash);
      target = target.length ? target : $('[name=' + this.hash.slice(1) +']');
      if (target.length) {
        $('html, body').animate({
          scrollTop: target.offset().top
        }, 1000);
        return false;
      }
    }
  });
});
</script>

<div class="edu-block">
  <div class="big-headline">
  We're so glad you're here, <%= @user.name %>! Ready to invest?
  </div>

  <div class="headline">Our mission is to make it easier for people to get started investing.
  </div>
  <br />
  <a href="#anchor">Continue</a>
</div>


<a name="anchor" id="anchor" class="anchor-style">Investing</a>
<div class="edu-block">
  <div class="edu-para">
    <p>Investing is an awesome, safe way to grow your money over a long period of time. </p>

    <p>There's a lot of confusing information out there so we'll start by going over a few basics. Feel free to skip over any parts you already know. :)</p>
  </div>
 
    <%= link_to "Next", next_wizard_path, class:"next-button" %>
</div>

