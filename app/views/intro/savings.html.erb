<script type="text/javascript">
    mixpanel.track("savings", {
        
    });
</script>

<body class="page-background">

<section id="coach-section">
	<div class="coach-question">
		<aside>
		Enough about debt – let’s give your piggy bank a shake. 
		</aside>
		<p class="main-question">Roughly, how much have you saved up?</p>
		
	</div>
</section>

<!-- fade in user input section -->
<script>
$(document).ready(function(){
    $('div.hidden').hide(0).delay(1500).fadeIn(1000).removeClass('hidden');
});
</script>

<div class="hidden" style="display:none;">
	<section id="user-section">
	<%= form_for @user, url: wizard_path, method: :put do |f| %>
		<div class="spaced-row user-content">
			<span class="madlib-sentence">	
				<span class="dropdown-style inner-addon right-addon">
					<%= f.select(:savings_amount, [['I have hundreds', 1], ['I have thousands', 2], ['I have tens of thousands', 3], ['Someone raided my piggy bank', 0]], {prompt: 'My savings...'}, :required => true) %>
				<span class="glyphicon"><%= image_tag("dropdown-arrow.png")%></span>
				</span>
			</span>
		</div>
		<div class="button-row">
			<%= f.submit "Next", class: 'next-button' %>
		</div>
	</section>
</div>
<% end %>
</body>
