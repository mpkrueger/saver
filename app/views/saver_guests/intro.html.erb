<% @title=": Introductions" %>


<script>
$(document).ready(function() {
  $('a[href*="#us"]:not([href="#"])').click(function() {
    if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'') && location.hostname == this.hostname) {
      var target = $(this.hash);
      target = target.length ? target : $('[name=' + this.hash.slice(1) +']');
      if (target.length) {
        $('html, body').animate({
          scrollTop: target.offset().top
        }, 1000);

        
        return false;
      }
    }
  });
  $('a[href*="#you"]:not([href="#"])').click(function() {
    if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'') && location.hostname == this.hostname) {
      var target = $(this.hash);
      target = target.length ? target : $('[name=' + this.hash.slice(1) +']');
      if (target.length) {
        $('html, body').animate({
          scrollTop: target.offset().top
        }, 1000);

        // $('#stocks').hide(0).fadeIn(100).removeClass('hidden');
        
        return false;
      }
    }
  });
});

</script>

<div class="edu-block">
  <div class="intro_image_block"><%= image_tag("savvy_thumbnail.png", style:"width:150px;height:auto;") %> </div>
    <div class="big-headline">Hi there! </div>

    <div class="headline option-row">Ready to trim your bills?</div>
   <br /><a href="#us", class="next-button">Let's do it!</a>
</div>

<a name="us"></a>
<div class="edu-block">
    <div class="headline">Ok, here's how this works. We'll ask you a couple questions, we'll look at your latest cable/internet bill and then we'll negotiate you a lower rate without making any changes to your service. Easy money, right?
    </div>
    <br />
  <a href="#you", class="next-button">Yeah!</a>
  </div>
</div>


<%= form_for @saver_guest do |f| %>
<a name="you"></a>
<div class="edu-block">        
	<div class="headline">First off, a couple quick questions so we can give you an estimate of how much we can save you. </div>
	<br />
	<div style="margin-left:10px;">
	    <div class="headline">1. When did you last move? </div>
	    <div class="dropdown-style inner-addon right-addon">
	        <%= f.select :last_moved, [['less than a year ago', 'less than a year ago'], ['more than a year ago', 'more than a year ago']], { include_blank: "this long ago..." }, {:id => "dropdown", :required => true, :class => 'my-dropdown select'} %>
	         <span class="glyphicon" style="margin-top:-5px;">
	     		<%= "\u25BE" %>
	   		</span>
	    </div>
	    <br /><br />
	    <div class="headline">2. Have you negotiated a lower rate in the last year? </div>
	    <div class="dropdown-style inner-addon right-addon"> 
	        <%= f.select :negotiated, [['yes', 'yes'], ['no', 'no']], { include_blank: "I negotiated..." }, {:id => "dropdown", :required => true, :class => 'my-dropdown select'} %>
	        <span class="glyphicon" style="margin-top:-5px;">
	     		<%= "\u25BE" %>
	   		</span>
	    </div>
	    <br /><br />
	    <div class="headline">3. Which services do you currently have? </div>
	    <%= f.fields_for :services do |i| %>
	        <label><%= i.check_box :cable %> Cable</label><br />
	        <label><%= i.check_box :internet %> Internet</label><br />
	        <label><%= i.check_box :phone %> Phone </label><br />
	        <label><%= i.check_box :other %> Other </label>
	    <% end %>
    	<br /><br />
    </div>
    <%= f.submit "Next", class: 'next-button' %>
</div>

  <% end %>